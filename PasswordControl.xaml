<UserControl x:Class="CorpseLib.Wpf.PasswordControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CorpseLib.Wpf"
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="800">
    <DockPanel LastChildFill="True">
        <Button x:Name="ShowPassword" DockPanel.Dock="Right" Width="{Binding ActualHeight, RelativeSource={RelativeSource Self}}" Click="ShowPassword_Click"/>
        <local:PasswordBox x:Name="PasswordBox" DockPanel.Dock="Left" />
    </DockPanel>
</UserControl>
